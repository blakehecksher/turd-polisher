# 2026-05-18 2053 - Git Ignore

## What was done
- Replaced the old `.gitignore` that ignored every PNG with repo-specific rules.
- Ignored OS/editor noise, `.playwright-mcp/`, and the root-level generated screenshot files used during local testing.
- Removed the already-staged generated artifacts from the Git index so the new ignore rules take effect.

## What worked
- The new ignore rules match the current single-file/procedural structure better than a blanket asset ignore.
- `git rm --cached` removed the staged junk without deleting local files.

## What didn't and why
- Initial `git rm --cached` failed on `.git/index.lock` permissions and had to be retried with elevated git access.

## Decisions made
none

## Left unfinished
- none

## state.md updated: yes
